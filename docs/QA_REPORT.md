# 🛡️ 特價情報聚合平台 — QA 驗收與測試報告

> **專案名稱**：全通路特價情報聚合平台 (Pinterest 瀑布流 × 簡約泡泡風)  
> **更新內容**：v1.20.0 全功能會員系統、Google SSO、Email 登入與 Prisma 後端資料庫持久化  
> **驗收日期**：2026-08-31  
> **驗收狀態**：✅ 全數通過 (PASS)  
> **負責角色**：`@QA`

---

## 📋 一、 驗收走查矩證 (Acceptance Verification)

| 驗收條目 (AC) | 測試情境與操作 | 預期結果 | 測試結論 |
| :--- | :--- | :--- | :--- |
| **AC-44**<br>🧹 特價與新品情報品質治理與防幻覺清洗 (Hallucination Purge & Gatekeeper) | 1. 檢驗全站特價情報是否仍殘留純粉專首頁導流網址（如 `facebook.com/<brand>` 且點擊無特定促銷）。<br>2. 檢驗食尚玩家食記文章是否殘留「私人招待所風格」、「菜單、價位一覽」、「【優惠看這裡】」等非商品之裝潢/分類標題幻覺。<br>3. 檢驗爬蟲雜訊是否殘留粉專自身名稱。<br>4. 檢驗修復之開學繳費與微醺特惠活動貼文。<br>5. 檢驗全站高品質新品（麝香葡萄大福、木瓜牛奶霜淇淋）與多元促銷活動（買1送1、加價購、滿額贈、吃到飽）完整保留。<br>6. 驗證爬蟲入庫品管守門員 (`isValidQualityDeal`) 是否成功阻擋不良貼文。 | 1. 成功永久刪除 58 筆純首頁虛構與食記非商品裝潢/菜單切片情報，全站剩餘 67 筆情報 100% 具備具體活動貼文或專屬報導連結，無任何純首頁導流。<br>2. 徹底消除所有非商品之文章裝潢形容詞與假價格（$170）幻覺。<br>3. 修復 2 筆具備真實官方活動貼文之開學送姓名貼與微醺巧克力特惠卡片。<br>4. 完整保留新品上市情報與多元促銷活動，精準契合「消費特惠與新品情報雷達」定位。<br>5. `gemini-parser.service.ts`、`blog-crawler.service.ts`、`run-scheduled-crawl.mjs` 守門員機制順利阻擋純首頁與爬蟲雜訊。<br>6. `npm run build` 通過。 | ✅ **PASS** |
| **AC-41**<br>🎴 優惠卡片獨立拆解 (1-to-N Splitting) | 1. 檢視社群多圖/多商品促銷貼文（如美廉社週末促銷，包含文旦、杜老爺冰棒、SAJO飯捲、農搾、林鳳營鮮乳共 5 款商品）。<br>2. 驗證系統是否拆解為 5 張各自獨立的 `SmartDeal` 卡片，每張卡片皆展示專屬商品圖。<br>3. 驗證已移除舊版合併打包之卡片。 | 1. 成功將 1 篇貼文之 5 款商品拆分為 5 筆完全獨立的特惠卡片。<br>2. 每張卡片皆具備獨立標題、專屬商品圖片與精確起訖日 (09/04~09/06)。<br>3. 舊版 5 圖合一卡片 `crawled-simplemart-e7be8ee5bb89e7a4` 已完全移除。 | ✅ **PASS** |
| **AC-42**<br>🏷️ 特惠機制直接顯示（買一送一/全館折數免標特價金額） | 1. 檢查杜老爺脆冰棒與 SAJO 飯捲卡片價格呈現方式。<br>2. 檢查林鳳營全脂鮮乳卡片價格呈現方式。<br>3. 檢驗彈窗 `DealDetailModal` 與卡片 `SmartDealCard` 雙端呈現。 | 1. 買一送一商品在卡片主價格區直接大字醒目呈現「買1送1」，不再顯示生硬折算金額或模糊的「促銷特惠/件」，原價以刪除線輔助提示。<br>2. 固定特價商品（林鳳營鮮乳）清楚呈現「NT$ 159」與原價「NT$ 194」。<br>3. 彈窗頂部徽章與價格氣泡同步套用機制文字，視覺清晰直覺。 | ✅ **PASS** |
| **AC-43**<br>🏢 品牌標籤 100% 固定規範收斂與歷史資料庫清洗 | 1. 檢驗全站品牌標籤標準化（美廉社 ➔ `#美廉社`；全家 ➔ `#全家`；7-11 ➔ `#7-11`；好市多 ➔ `#好市多`）。<br>2. 檢驗 DAL 讀取層 `mapDbDealToSmartDeal` 是否內建防禦性正規化。<br>3. 執行全資料庫歷史資料清洗腳本，檢驗是否殘留 `#美廉社 Simple Mart` 等未標準化標籤。 | 1. 全站品牌標籤嚴格收斂至固定標準 Tag，杜絕任何冗長雙語或別名標籤。<br>2. DAL 讀取層具備即時正規化過濾，徹底防止髒標籤洩漏至前端畫面。<br>3. 歷史資料庫清洗完畢，全站 126 筆特惠情報標籤全數合規。 | ✅ **PASS** |
| **AC-39**<br>📱 最高管理後台 Sidebar 側邊欄重構與獨立子路由架構 | 1. 檢驗桌面端（`>= 1024px`）左側是否呈現 260px 簡約專業 Sidebar 側邊欄，支援分類導覽（情報維運、管理安全）。<br>2. 檢驗移動端（`< 1024px`）頂部漢堡選單觸發之 Drawer 抽屜視圖，並驗證 Safe Area (`env(safe-area-inset-top)` / `env(safe-area-inset-bottom)`)。<br>3. 驗證 `/admin` 拆分獨立子頁面：`/admin` (總覽)、`/admin/deals` (特價卡片)、`/admin/crawler` (爬蟲排程)、`/admin/ads` (廣告全域監控)、`/admin/permissions` (管理權限編輯)、`/admin/settings` (系統安全設定)。<br>4. 驗證桌面端頂部麵包屑路徑連動與全站快速鎖定/返回前台連結。 | 1. 桌面端左側 Sidebar 固定顯示，當前選中項目以黑底深色膠囊高亮，並提供狀態指示燈。<br>2. 移動端漢堡選單點擊滑出 Drawer 抽屜，點擊導覽項目自動收合抽屜並伴隨 Native 觸覺震動。<br>3. 所有子頁面皆具備獨立 URL 路由，支援瀏覽器書籤收藏、直接輸入網址與前進/後退歷史紀錄。<br>4. `AdminShell` 統一管理全域 `AdminAuthGuard` PIN 碼鎖定屏，驗證通過後全局無縫解鎖。 | ✅ **PASS** |
| **AC-40**<br>🛡️ 管理權限編輯與成員名冊 (`/admin/permissions`) 及系統安全設定 (`/admin/settings`) | 1. 進入 `/admin/permissions` 檢驗使用者統計數據卡片（總人數、ADMIN、MERCHANT、USER）。<br>2. 測試以姓名、Email 即時搜尋成員，以及按角色分類標籤快速過濾。<br>3. 測試下拉選單變更成員角色（USER ➔ MERCHANT ➔ ADMIN），檢查是否彈出防呆確認對話框，並提供 Optimistic UI 即時更新。<br>4. 點擊「檢視權限階層說明矩陣」彈窗，檢驗各角色權責對照表。<br>5. 進入 `/admin/settings` 測試線上變更最高管理 PIN 碼，並測試開關「快速展示 8888 演示解鎖模式」。 | 1. 使用者名冊即時查詢 Neon PostgreSQL，清晰展示頭像、姓名、Email、註冊來源、收藏與標籤數。<br>2. 關鍵字即時搜尋與角色過濾流暢，查無資料時展示友善 Empty 態與一鍵重置按鈕。<br>3. 變更角色時彈出確認對話框，清楚揭示 ADMIN / MERCHANT 權責風險，確認後資料庫即時更新並自動寫入稽核日誌。<br>4. 權限矩陣對照表清晰標示各功能讀寫範圍。<br>5. 安全 PIN 碼支援舊碼驗證與新碼防呆修改；關閉展示模式後，8888 一鍵解鎖按鈕自動阻擋並提示需輸入真實 PIN 碼。 | ✅ **PASS** |
| **AC-37**<br>🤖 爬蟲伺服器排程常駐運行與真實 PostgreSQL 日誌持久化 | 1. 檢驗 `prisma/schema.prisma` 新增之 `CrawlerLog`、`CrawlerTarget`、`CrawlerSchedule` 模型並成功推送至 Neon PostgreSQL。<br>2. 驗證 `src/instrumentation.ts` 伺服器啟動掛鉤，確認 Node.js 環境開機自動啟動常駐排程 Daemon。<br>3. 驗證 `crawler-scheduler-engine.ts` 每分鐘比對黃金時段、週四衝刺波與整點過期巡檢。<br>4. 驗證後台日誌區徹底拔除 Mock 假資料，全數直接查詢與寫入 PostgreSQL，重整伺服器日誌真實不丟失。 | 1. Prisma 遠端資料表建立完成，自動種子化預設站點與全域排程。<br>2. 伺服器開機常駐 Daemon 順利執行單例檢查，各站點 `lastCrawledAt` 與 `lastStatus` 真實持久化。<br>3. 伺服器每小時自動執行過期巡檢與排程抓取，成功記錄至 `CrawlerLog`。<br>4. 後台展示真實時間戳與真實入庫/採集筆數，徹底告別假日誌。 | ✅ **PASS** |
| **AC-38**<br>🚀 即時爬蟲視覺化處理狀況中樞 (`CrawlerProgressModal`) 與 SSE 實時串流 | 1. 測試點擊「全通路全量抓取」、「批量立即抓取」、「單站即時抓取」或「食尚玩家文章即時採集」。<br>2. 驗證是否立即彈出 `CrawlerProgressModal`。<br>3. 驗證實時已耗時計時器 (mm:ss) 遞增、進度百分比條 (0% ~ 100%) 動態推進。<br>4. 檢驗當前站點卡片（Logo、名稱、四階段指示燈：1.連線 ➔ 2.擷取 ➔ 3.Gemini AI 拆解 ➔ 4.入庫去重）。<br>5. 檢驗自動置底的高對比終端機日誌串流與完成後成果摘要卡片。 | 1. 點擊即時爬蟲按鈕立即跳出高對比全屏處理狀況 Modal，徹底消除無狀態盲區。<br>2. 計時器與進度條流暢連動，SSE 串流毫秒級回傳各站點進度事件。<br>3. 站點切換流暢，四階段指示燈清晰標註當前動作。<br>4. 終端機色彩高亮即時事件日誌並自動滾動至最新行。<br>5. 完成後展示入庫筆數總結、最新卡片縮圖預覽，並提供「前往情報牆」無縫跳轉。 | ✅ **PASS** |
| **AC-36**<br>🛡️ 畫面瀏覽防跳動與資料確實讀取載入機制 | 1. 於桌機寬螢幕重整首頁檢測 Hydration 是否發生 2 欄至 4 欄之重新分派飛位。<br>2. 於頂部副標籤列切換「咖啡」、「買一送一」、「國泰CUBE」等標籤，檢測是否會先閃爍 1~2 張暫存殘影。<br>3. 檢視卡片大圖異步載入時，是否因高度坍塌推擠下方卡片（CLS 檢測）。<br>4. 測試手勢左右滑動切換 Tab 時，外層 DOM 容器是否保持平穩轉場無閃白。 | 1. 未掛載/計算欄數前由 `DealMasonrySkeleton`（CSS 響應式 4 欄）平滑佔位，掛載確認欄數後真實卡片平滑淡入，完全消除跨欄跳躍。<br>2. 徹底移除 0ms 不完整暫存閃爍邏輯，切換標籤時進入 Loading 態展示骨架屏，待伺服器最新分頁資料確實讀取完畢後一次性穩定呈現真實畫面。<br>3. `SmartDealCard` 圖片容器全面預留 Aspect Ratio（預設 `4:3`），圖片下載完成直接在背景顯現，不推擠任何下方元素，CLS 降為 0。<br>4. 移除破壞性 `key={selectedTag}`，手勢與 Tab 切換維持原位平滑滑動，無閃白現象。 | ✅ **PASS** |
| **AC-33**<br>🐘 遠端資料庫 (Neon PostgreSQL + Prisma ORM) 遷移與真實 CRUD | 1. 驗證 `prisma/schema.prisma` 定義 `Deal` 與 `AdCampaign` 模型並同步至遠端 Neon PostgreSQL。<br>2. 驗證資料庫初始化自動將 87 筆精選特價情報寫入遠端資料庫。<br>3. 驗證前台首頁特價瀑布流直接從遠端資料庫查詢與篩選。<br>4. 驗證後台 `/admin` 單筆與批量修改、刪除、熱門/快閃切換即時持久化至遠端資料庫。<br>5. 驗證商家中心 `/merchant` 發布特價與建立廣告活動寫入遠端資料庫。<br>6. 重啟伺服器與 Next.js build 測試資料庫連線與持久化。 | 1. Prisma Client 與 Neon PostgreSQL 連線與 Schema 同步完成。<br>2. 自動種子化機制正常運作，遠端資料庫成功寫入初始特價與廣告資料。<br>3. 關鍵字、分類、通路、商圈與多重排序查詢皆從遠端資料庫返回。<br>4. 後台批量編輯價格、標籤、刪除與狀態切換皆即時更新至遠端資料庫，重整頁面維持修改後狀態。<br>5. 商家發布之特惠與廣告活動成功入庫，前台情報牆即時可見。<br>6. `npm run build` 通過，伺服器重啟資料不丟失。 | ✅ **PASS** |
| **AC-32**<br>🔐 全功能會員系統、Google SSO、Email 登入與 Prisma 後端資料庫持久化 | 1. 測試未登入時點擊右上角「登入 / 註冊」膠囊<br>2. 測試 Google SSO 一鍵登入<br>3. 測試 Email / 密碼雙軌註冊與登入（含 Zod 格式防呆與 bcrypt 雜湊）<br>4. 測試會員個人中心（`/profile`）數據展示<br>5. 測試自訂標籤與特惠收藏寫入 Prisma 資料庫<br>6. 測試會員登出與 Session Cookie 清除 | 1. 成功彈出 Pinterest 泡泡風 `AuthModal`，具備流暢切換與 UI 五態。<br>2. Google SSO 一鍵連線成功，建立/關聯 User 與 Account 紀錄並核發 JWT Session Cookie。<br>3. Email 註冊防呆檢驗嚴格，密碼雜湊安全入庫，成功即時登入並觸發彩帶動畫。<br>4. Header 右上角轉為 `UserMenuDropdown`，顯示頭像、姓名與管理捷徑。<br>5. 標籤修改與特惠收藏直接寫入資料庫（`bookmarks`, `user_subscription_tags`），重新整理資料不丟失。<br>6. 點擊登出安全銷毀 Cookie 並回到訪客模式。 | ✅ **PASS** |
| :--- | :--- | :--- | :--- |
| **AC-01**<br>第二行副 Header 極簡純文字標籤列 | 檢視頂部全域 Header 第二行 | 標籤列呈現極簡純文字橫向排列（`All 全部` / `買一送一` / `咖啡` / `星巴克` 等），無多餘膠囊底色與粗糙外框，維持純粹呼吸感。 | ✅ **PASS** |
| **AC-02**<br>Pinterest 實心底線指示態 | 點擊切換不同標籤或「All 全部」 | 當前啟用項目以粗體黑字搭配底部俐落實心黑線 (`border-b-2 border-slate-900`) 明確指示，未選中項目以低飽和灰字呈現。 | ✅ **PASS** |
| **AC-03**<br>標籤即時篩選與 Pinterest 畫板 Header | 點擊標籤（如「買一送一」） | 瀑布流即時過濾，Feed 頂部以 Pinterest 畫板風格展示「主題名稱 + 特惠則數 + 返回全部」按鈕。 | ✅ **PASS** |
| **AC-04**<br>自訂標籤快捷加入 | 點擊標籤列右側「+ 自訂標籤」輸入新標籤 | 支援即時加入自訂標籤至訂閱池，帶有紙花與震動反饋，並自動跳轉至該標籤篩選結果。 | ✅ **PASS** |
| **AC-05**<br>跨頁標籤導覽 | 於商家頁面點擊第二行副 Header 標籤 | 自動導回首頁並套用選中標籤篩選。 | ✅ **PASS** |
| **AC-06**<br>Embla Carousel 手勢與輪播核心 | 於首頁頂部輪播進行觸控/滑鼠拖曳與等待自動播放 | 具備極致順暢的 60fps 慣性滑動手勢、自動輪播定時器（5秒）、滑鼠懸停時自動暫停，支援鍵盤左右箭頭導航。 | ✅ **PASS** |
| **AC-07**<br>Header 全域搜尋列整合 | 於頂部導覽列輸入搜尋關鍵字 (如「星巴克」、「買一送一」) | 搜尋框即時連動 `dealFiltersAtom`，首頁瀑布流同步過濾資料；支援 `Esc` 與 `✕` 一鍵清除搜尋字詞。 | ✅ **PASS** |
| **AC-08**<br>Pinterest 質感大圖與資訊收斂 | 載入首頁情報瀑布流 | 卡片上方展示吸睛的大圖封面 (`rounded-2xl`)，圖片自帶折數與快閃膠囊；卡片外部資訊精簡不擁擠，呈現乾淨 Pinterest 風格。 | ✅ **PASS** |
| **AC-09**<br>🎯 條件與 💳 信用卡 Icon 膠囊化 | 檢查首頁各卡片下方標籤 | 複雜的長文字收斂為「🎯 條件 Icon 膠囊」與「💳 信用卡 Icon 膠囊」，多餘項目以 `+N` 簡潔標記。 | ✅ **PASS** |
| **AC-13**<br>探索地區商圈 Tag 多選器與全區互斥 | 於 `DealFilterModal` 點選 Section 2 商圈 | 1. 頂部原重複之固定泡泡列已移除，改為即時「已選商圈 Tag 列表」。<br>2. 當選取「全區門市」時，同縣市底下已選之個別行政區會自動清除。<br>3. 全區門市選取時，同縣市個別行政區轉為 Disabled 不可選狀態。<br>4. 每個 Tag 支援點擊 `✕` 獨立移除；「全部清空」按鈕可一鍵重置。<br>5. 點擊「套用篩選設定」正確依據多選地區進行情報過濾。 | ✅ **PASS** |
| **AC-14**<br>Modal Header Title 防畫面跳動 | 於 `DealFilterModal` 切換不同篩選條件 | 彈窗 Header 標題維持純文字靜態穩定排版，無動態已選項目 Badge 導致之寬度變化與跳動。 | ✅ **PASS** |
| **AC-15**<br>手機雙瀑布流與貪婪高度自動平衡 | 檢視手機端（`< 768px`）與各尺寸螢幕之情報牆排列 | 1. 手機端全面呈現雙欄瀑布流（`grid-cols-2`），雙列卡片緊湊精緻。<br>2. 實作貪婪高度平衡演算法：第一排由左至右展示最新特惠，後續卡片自動分派至「目前總高度最短的直欄」，確保兩欄高度均勻，徹底防止某一列過長失衡。<br>3. 支援 5 種卡片比例（1:1, 3:4, 4:3, 16:9, 9:16）動態權重計算。 | ✅ **PASS** |
| **AC-16**<br>5 大卡片圖比例規格與商家上傳標準 | 檢查首頁情報卡片與商家自主發布後台 | 1. 支援 `1:1`、`3:4`、`4:3`、`16:9`、`9:16` 共 5 種圖片比例標準。<br>2. `SmartDealCard` 正確依據比例渲染，圖片保持 `object-cover` 與圓角造型。<br>3. 商家發布表單提供 5 種規格按鈕與即時 Live Preview 響應。 | ✅ **PASS** |
| **AC-17**<br>手機端卡片極限瘦身與精緻化 | 於手機視圖（雙瀑布流）檢視卡片資訊層級 | 1. 內距縮減為 `p-2`，圓角適配 `rounded-2xl`。<br>2. 標題、價格、條件 Icon 膠囊微調為手機專屬比例，杜絕雙欄下的折行與擁擠。<br>3. 點擊卡片依然可開啟完整 7 大要素彈窗。 | ✅ **PASS** |
| **AC-18**<br>手機端手勢左右滑動切換 Tab | 於手機觸控螢幕上向左/向右滑動情報牆區塊 | 1. 向左滑動（Swipe Left）：流暢切換至下一個標籤 Tab。<br>2. 向右滑動（Swipe Right）：流暢切換至上一個標籤 Tab。<br>3. 觸發震動反饋，頂部 `SubHeaderTagNav` 自動將當前選中之 Tab 平滑置中滾動。<br>4. 垂直正常滾動頁面時不誤觸切換。 | ✅ **PASS** |
| **AC-19**<br>公開優惠情報 API 串接與多來源轉接器 | 測試 PTT Lifeismoney 爬蟲、開放電商特價池與 DAL 聚合快取 | 1. 成功即時解析 PTT 省錢板最新爆文與情報，自動識別品牌店家並關聯高品質情境封面。<br>2. 成功介接開放商品折扣 API 並換算為台幣 NTD 與折數。<br>3. 5 分鐘記憶體快取與 ISR 增量靜態生成正常運作，確保頁面毫秒級順暢載入。<br>4. 情報牆頂部「即時公開情報連線」狀態列正常運作，「重新整理公開情報」按鈕觸發手動更新與紙花反饋。<br>5. 卡片來源 Badge（PTT 爆文 / 即時特惠）與詳情彈窗「查看原始情報網頁」外連按鈕運作正常。 | ✅ **PASS** |
| **AC-20**<br>外部 API 移除與 PTT/爆文標籤徹底清理 | 檢查全站元件、DAL 與 Schema 是否已無外部爬蟲依賴與 PTT 關鍵字 | 1. 刪除 `ptt-lifeismoney.adapter.ts` 與 `consumer-deals.adapter.ts`，DAL 重構回歸純淨內部資料層。<br>2. 卡片右上角來源標籤已全數移除，回歸極簡乾淨 Pinterest 封面。<br>3. 情報牆頂部「即時公開情報連線」橫幅已移除，畫面排版更專注於高質感特惠大圖。<br>4. 全站 Types、Schema、Mock 資料與頁面文字已 100% 清理所有 PTT / 爆文詞彙。<br>5. `npm run build` 通過，無任何型別錯誤。 | ✅ **PASS** |
| **AC-21**<br>優惠詳情來源快速導航與開發者查驗 | 點擊卡片開啟 `DealDetailModal` 檢驗來源區塊與導航按鈕 | 1. 詳情彈窗內新增「情報來源與查驗連結」卡片，正確顯示來源管道（7-11 粉專 / 全家粉專等）與社群讚數 / 留言數。<br>2. 點擊「複製網址」即時複製 `sourceUrl` 至剪貼簿，按鈕呈現「已複製」綠色成功態。<br>3. 點擊「開啟來源」或底部「前往官方貼文 / 來源頁」順利以新分頁開啟對應連結，並帶觸覺反饋。<br>4. 實體門市與線上通路雙端按鈕佈局流暢，無網址時友善顯示 Disabled 態。 | ✅ **PASS** |

| **AC-24**<br>智慧階梯式爬蟲排程與四大黃金窗口 | 檢視後端自動爬蟲服務與 Playwright Headless 引擎 | 1. 支援 4 大黃金窗口 (`08:30`, `12:00`, `18:00`, `21:30`) 與週四衝刺窗 (`17:00 ~ 19:00`)。<br>2. 深夜靜默時段 (`01:00 ~ 07:30`) 停止瀏覽器背景運作。<br>3. 支援異步排程與手動同步自測雙軌。 | ✅ **PASS** |
| **AC-25**<br>🛡️ 最高管理員控制台與安全 PIN 防護 | 進入 `/admin` 頁面測試權限鎖與功能操作 | 1. 未驗證時顯示安全 PIN 碼輸入介面，輸入錯誤 PIN 阻擋，輸入 `8888` 順利解鎖進入。<br>2. 提供全站特價卡片 CRUD，可即時編輯標題、價格、品項、條件，支援一鍵刪除與熱門/快閃切換。<br>3. 支援一鍵登出/鎖定後台。 | ✅ **PASS** |
| **AC-26**<br>🤖 爬蟲目標站點管理與手動即時抓取 | 於 `/admin` 爬蟲中控分頁切換站點開關與觸發爬蟲 | 1. 站點清單清晰展示超商、量販、星巴克、速食與手搖飲各通路，點擊 Toggle 開關可即時啟用/停用。<br>2. 支援儲存自訂排程時間（黃金波段、週四衝刺、深夜靜默）。<br>3. 點擊「單站抓取」或「⚡ 一鍵全通路即時爬取」，後端即時抓取並將新卡片寫入資料庫，日誌視窗即時滾動輸出，並於頂部呈現解析預覽卡片。 | ✅ **PASS** |
| **AC-27**<br>🏢 官方小編品牌隔離與 DM 批量快速製卡 | 於 `/merchant` 切換品牌與進行 DM 批量上傳 | 1. 官方小編工作台具備品牌切換功能（星巴克、全家、7-11、全聯等），僅列出該品牌的優惠活動與數據。<br>2. 支援加入多張促銷 DM 海報圖檔或 URL，點擊「✨ 啟動 AI 智能辨識」自動由海報中拆解出多筆結構化特惠卡片。<br>3. 支援草稿卡片即時微調，點擊「🚀 一鍵批量發布」觸發彩帶動畫並即時上線至情報牆。 | ✅ **PASS** |
| **AC-28**<br>📢 廣告版面購買系統 (Google/FB 流量競價模式) | 於小編後台建立廣告活動與測試預算試算 | 1. 支援首頁頂部大橫幅 (Hero Banner)、瀑布流贊助卡片與分類置頂 3 種展位。<br>2. 支援 CPM (NT$ 120/千次) 與 CPC (NT$ 3.5/點擊) 雙競價模式，調整每日預算與天數時，右側流量試算器即時動態推算預估曝光數、點擊數與預算總計。<br>3. 點擊確認下單成功建立廣告活動，並可於投放管理清單即時暫停/恢復投放。 | ✅ **PASS** |
| **AC-29**<br>🖥️ 後台獨立 Layout 分流介面 | 進入 `/admin` 與 `/merchant` 頁面驗證 Layout 結構 | 1. 前台搜尋框、探索標籤列與消費端 Footer 完全隱藏。<br>2. `/admin` 呈現專屬深色 Super Admin 控制台 Header，具備系統狀態與返回前台連結。<br>3. `/merchant` 呈現專屬翡翠綠 Official Editor 品牌 Studio Header 與品牌身份徽章。<br>4. 前台首頁 `/`、`/profile`、`/tag/[tagName]` 正常保有完整搜尋 Header 與 Footer。 | ✅ **PASS** |
| **AC-30**<br>⚙️ 爬蟲網站自訂新增、單站獨立排程與多選批量操作 | 於 `/admin` 測試新增爬蟲網站、單站編輯與多選批量排程 | 1. 點擊「新增爬蟲網站」彈窗可成功加入自訂站點（名稱、URL、分類、自訂排程與規則）。<br>2. 每個站點具備「排程」按鈕，可單獨設定為「跟隨全域」、「自訂時段 (如 09:00, 15:00)」或「固定間隔分鐘數」。<br>3. 支援站點 Checkbox 複選與「全選」，懸浮工具列支援「批量啟用」、「批量暫停」、「批量設定排程」與「批量立即抓取」。 | ✅ **PASS** |
| **AC-33**<br>☁️ 遠端 Neon 雲端 PostgreSQL 資料庫無縫遷移 | 檢驗 Prisma ORM 遠端讀寫與自動補齊 103 筆種子特價情報 | 1. 成功於遠端 Neon Serverless PostgreSQL 建立 `Deal` 與 `AdCampaign` 資料表並部署 Schema。<br>2. 整合 DAL 層，全站特價卡片 CRUD、熱門/快閃切換、廣告活動與爬蟲寫入全數改由遠端 PostgreSQL 即時處理。<br>3. 支援資料庫自動種子資料與過期特價自動清理 (`purgeExpiredDeals`)。 | ✅ **PASS** |
| **AC-34**<br>📄 特價卡片清單分頁換頁與每頁筆數自訂 | 於 `/admin` 與 `/merchant` 測試分頁導航與 20/50/100/全部筆數切換 | 1. 預設每頁顯示 20 筆，底部清楚顯示 `顯示第 1 - 20 筆，共 79 筆`。<br>2. 點擊頁碼 `2`、`3` 或「下一頁」/「最末頁」流暢切換頁面，資料切片準確無誤。<br>3. 切換下拉選單為「50 筆 / 頁」、「100 筆 / 頁」或「全部顯示」，分頁即時動態重算總頁數與區間。<br>4. 輸入關鍵字或變更分類篩選時，頁碼自動重置回第 1 頁。<br>5. 全選 Checkbox 支援當前頁面選取，避免跨頁誤操作。 | ✅ **PASS** |
| **AC-35**<br>🛡️ 多選 Header 防跳動與新增後自動即時重整 | 測試卡片勾選/取消勾選、手動發布新卡片即時同步 | 1. 勾選/取消勾選特價卡片時，Header 高度固定 (`min-h-[60px]`)，「已選取 X 筆」與批量按鈕平滑淡入，完全消除高度跳動。<br>2. 新增或發布特價卡片後，下方清單透過 `useEffect` 與 `router.refresh()` 即時同步最新資料，無需重新載入網頁。 | ✅ **PASS** |

---

## 🎨 二、 前端 UI 積木五態檢驗

* ⏳ **Loading 態**：`DealMasonrySkeleton`、AI 辨識動畫與即時爬蟲 Loading 旋轉態。
* 📭 **Empty 態**：品牌卡片與廣告清單無資料時提供引導說明與一鍵建立按鈕。
* ⚠️ **Error 態**：PIN 驗證失敗、表單檢核錯誤與網路異常友善提示。
* ✅ **Success 態**：批量製卡與廣告建立觸發彩帶特效 (Confetti) 與 Toast 回饋。
* 🔒 **Disabled/Active 態**：執行中按鈕防重複點擊，支援 Mobile 觸覺反饋。

* ✅ **Success 態**：Pinterest 質感大圖瀑布流順暢排列。
* 🔒 **Disabled/Active 態**：按鈕防連擊與點擊縮放微動效。

| **AC-37**<br>📰 食尚玩家與多品牌部落格情報爬蟲、精準分類、後台部落格模式增刪與粉專優先衝突保護機制 | 1. 爬取食尚玩家 360820（六扇門小火鍋專題）測試多品牌與分店解析。<br>2. 測試 1 篇綜合整理文章拆解為多個獨立品牌卡片（1-to-N）。<br>3. 測試粉專衝突優先權：既有粉專第一手情報不被部落格轉載覆蓋。<br>4. 測試後台爬蟲中控台「部落格/專題媒體」篩選分頁籤、單篇文章 URL 即時採集工具箱與自訂新增部落格站點。 | 1. 成功解析六扇門小火鍋（170元起、火鍋分類、4間指定門市、#六扇門 #吃到飽 #24小時 5層標籤）。<br>2. 多品牌綜合文章精準拆解為獨立品牌卡片，無跨店家混淆。<br>3. 衝突優先級檢核通過（`social_listening` > `official` > `blog_curation`），粉專一手資料優先保留。<br>4. 後台提供單篇 URL 即時採集工具箱與部落格站點增刪開關，全數 PASS！ | ✅ **PASS** |
